<form method="post" action="{{ path('app_contact_delete', {'id': contact.id}) }}" onsubmit="return confirm('Attention cette demande de devis sera  supprimé?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact.id) }}">
     <button class="btn badge bg-danger fs-6 mx-2 my-1">Suprimer</button>
</form>