<form method="post" action="{{ path('app_frequency_delete', {'id': frequency.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frequency.id) }}">
    <button class="btn-delete">Delete</button>
</form>
