{% extends 'base.html.twig' %}

{% block title %}Frequence{% endblock %}

{% block body %}
    <h1>listes des frequances</h1>

   <table class=" table ">
        <thead>
            <tr>
            
                <th scope="col">Type</th>
                <th scope="col">action</th>
            </tr>
        </thead>
        <tbody >
        {% for frequency in frequencies %}
            <tr >
               
                <td  >{{ frequency.type }}</td>
                <td class="edition">
          <a class="btn"  href="{{ path('app_frequency_edit', {'id': frequency.id}) }}">edit </a>
                       {{ include('frequency/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
        <a class="btn" href="{{ path('app_frequency_new') }}">Ajouer</a>

{% endblock %}