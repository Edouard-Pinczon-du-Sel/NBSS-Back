{% extends 'base.html.twig' %}

{% block title %}Frequency{% endblock %}

{% block body %}
    <h1>Frequency</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ frequency.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ frequency.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_frequency_index') }}">back to list</a>

    <a href="{{ path('app_frequency_edit', {'id': frequency.id}) }}">edit</a>

    {{ include('frequency/_delete_form.html.twig') }}
{% endblock %}
