{{ form_start(form) }}
    <div class="text-dark">
        {{ form_widget(form) }}
    </div>
        <button class="btn btn-dark mt-2">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
